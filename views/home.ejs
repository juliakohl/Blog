<%- include("partials/header") -%>
<h1>Home</h1>
<% postList.forEach(function(post){ %>
    <% let preview = post.body.substring(0, 100); %>
    <h2> <%= post.title %> </h2>
    <p> <%= preview %> ... <a href="/posts/<%= post.title %>"">Weiterlesen</a></p>
<% }); %>
<%- include("partials/footer") -%>
